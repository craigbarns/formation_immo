# Vidéos hébergées localement

Placez ici les fichiers référencés dans `src/data/course.ts` (chemins `/videos/nom.mp4`).

- **`loi-alur-intro.mp4`** — première vidéo (intro D-ID, mode `--short`). Régénérable avec `node scripts/did-generate-lesson-01.mjs --short` puis téléchargement du MP4 depuis l’URL retournée.

Les liens **S3 D-ID expirent** : privilégiez ce dossier en dev ou **YouTube non listé** en production.
